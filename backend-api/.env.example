# Server
PORT=3000
NODE_ENV=production

# CORS
CORS_ORIGIN=https://nexoleads.inmoworkingpro.es

# Authentication
LOGIN_PASSWORD=nexoleads123

# Optional: SerpAPI for enhanced Google Maps reviews (requires paid tier)
# SERPAPI_KEY=your_serpapi_key

# Database (future: para persistencia)
# DATABASE_URL=postgresql://user:password@localhost/radares_db
